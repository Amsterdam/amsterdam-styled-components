import { Fragment } from 'react'
import styled from 'styled-components'
import { Meta, Story, Canvas } from '@storybook/addon-docs'
import { themeSpacing, themeColor } from '@amsterdam/asc-ui'

<Meta title="Utils/themeSpacing" component={themeSpacing} />

# themeSpacing

A function to retrieve a value derived from the theme spacing unit

## Default

<Canvas>
  <Story name="Default">
    <Fragment>
      {(() => {
        const MyThemeSpacedDiv = styled.div`
          color: ${themeColor('tint', 'level1')};
          background-color: ${themeColor('primary')};
          padding: ${themeSpacing(2)};
        `
        return (
          <MyThemeSpacedDiv>
            I have a padding of 8px (themeSpacing(2), as our spacing unit is
            4px, so 2 x 4 = 8px)
          </MyThemeSpacedDiv>
        )
      })()}
    </Fragment>
  </Story>
</Canvas>
