import { Fragment } from 'react'
import styled from 'styled-components'
import { Meta, Story, Canvas } from '@storybook/addon-docs'

<Meta title="Assets/Fonts" />

# Fonts

## Variations

<Canvas>
  <Story name="Variations">
    <Fragment>
      {(() => {
        const Regular = styled.p`
          font-weight: 400;
        `
        const Bold = styled.p`
          font-weight: 700;
        `
        const ExtraBold = styled.p`
          font-weight: 800;
        `
        return (
          <Fragment>
            <Regular>Amsterdam Sans Regular: font-weight: 400</Regular>
            <Bold>Amsterdam Sans Bold: font-weight: 700</Bold>
            <ExtraBold>Amsterdam Sans ExtraBold: font-weight: 800</ExtraBold>
          </Fragment>
        )
      })()}
    </Fragment>
  </Story>
</Canvas>
