import { Meta, Story, Canvas } from '@storybook/addon-docs'
import { SearchBar } from '@amsterdam/asc-ui'

<Meta
  title="UI/SearchBar"
  component={SearchBar}
  decorators={[
    (Item) => (
      <div
        style={{
          maxWidth: '400px',
        }}
      >
        <Item />
      </div>
    ),
  ]}
  argTypes={{
    value: {
      control: {
        type: 'text',
      },
    },
    onChange: {
      action: 'onChange triggered',
    },
    onClear: {
      action: 'onClear triggered',
    },
    onSubmit: {
      action: 'onSubmit triggered',
    },
  }}
/>

# SearchBar

Doesn't exist in Design System yet

**Component status: Experimental**

## Default

<Canvas>
  <Story name="Default">
    {(args) => <SearchBar placeholder="Enter the search text" {...args} />}
  </Story>
</Canvas>

## With Focus

<Canvas>
  <Story name="With Focus">
    {(args) => (
      <SearchBar
        placeholder="This will focus automatically"
        autoFocus
        {...args}
      />
    )}
  </Story>
</Canvas>

## In A Form

<Canvas>
  <Story name="In A Form">
    {({ onChange, onSubmit }) => (
      <form onSubmit={onSubmit}>
        <SearchBar placeholder="Enter the search text" onChange={onChange} />
      </form>
    )}
  </Story>
</Canvas>
