import { Fragment } from 'react'
import { action } from '@storybook/addon-actions'
import { Meta, Story, Preview } from '@storybook/addon-docs/blocks'
import { Switch } from '@amsterdam/asc-ui'

<Meta
  title="UI/Switch"
  component={Switch}
  decorators={[
    (storyFn) => (
      <div
        style={{
          display: 'flex',
          maxWidth: 400,
          flexDirection: 'column',
          alignItems: 'flex-start',
        }}
      >
        {storyFn()}
      </div>
    ),
  ]}
/>

# Switch

[Design System](https://designsystem.amsterdam.nl/7awj1hc9f/p/647350-toggle)

**Component status: experimental**

## Default off state

<Preview>
  <Story name="Default">
    <Fragment>
      <Switch id="default" onChange={(e) => {  action(`switch pressed ${e.target.checked}`)(e)}} />
    </Fragment>
  </Story>
</Preview>

## Checked state

<Preview>
  <Story name="Checked">
    <Fragment>
      <Switch id="checked" defaultChecked="checked" />
    </Fragment>
  </Story>
</Preview>

