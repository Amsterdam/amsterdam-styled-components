import { Fragment } from 'react'
import { action } from '@storybook/addon-actions'
import { Meta, Story, Preview } from '@storybook/addon-docs/blocks'
import { Toggle } from '@amsterdam/asc-ui'

<Meta
  title="UI/Toggle"
  component={Toggle}
  decorators={[
    (storyFn) => (
      <div
        style={{
          display: 'flex',
          maxWidth: 400,
          flexDirection: 'column',
          alignItems: 'flex-start',
        }}
      >
        {storyFn()}
      </div>
    ),
  ]}
/>

# Toggle

[Design System](https://designsystem.amsterdam.nl/7awj1hc9f/p/647350-toggle)

**Component status: experimental**

## Default off state

<Preview>
  <Story name="Default">
    <Fragment>
      <Toggle id="default" left="Ja" right="Nee" onChange={(e) => { action(`toggle switched ${e.target.value}`)(e)}} />
    </Fragment>
  </Story>
</Preview>
