import { Fragment } from 'react'
import { action } from '@storybook/addon-actions'
import { Meta, Story, Preview } from '@storybook/addon-docs/blocks'
import { Toggle } from '@amsterdam/asc-ui'

<Meta
  title="UI/Toggle"
  component={Toggle}
  decorators={[
    (storyFn) => (
      <div
        style={{
          display: 'flex',
          maxWidth: 400,
          flexDirection: 'column',  
          alignItems: 'flex-start',
        }}
      >
        {storyFn()}
      </div>
    ),
  ]}
/>

# Toggle

[Design System](https://designsystem.amsterdam.nl/7awj1hc9f/p/647350-toggle)

**Component status: experimental**

## Default - ja

<Preview>
  <Story name="Default-ja">
    <Fragment>
      <Toggle id="default-ja" defaultValue="Ja" left="Ja" right="Nee" onChange={(e) => { action(`toggle switched ${e.target.value}`)(e)}} />
    </Fragment>
  </Story>
</Preview>

## Default - nee

<Preview>
  <Story name="Default-nee">
    <Fragment>
      <Toggle id="default-nee" defaultValue="Nee" left="Ja" right="Nee" onChange={(e) => { action(`toggle switched ${e.target.value}`)(e)}} />
    </Fragment>
  </Story>
</Preview>
