import { Meta, Story, Canvas } from '@storybook/addon-docs'
import { CompactPager } from '@amsterdam/asc-ui'

<Meta
  title="UI/CompactPager"
  component={CompactPager}
  argTypes={{
    page: {
      control: {
        type: 'number',
      },
    },
    collectionSize: {
      control: {
        type: 'number',
      },
    },
    pageSize: {
      control: {
        type: 'number',
      },
    },
    onPageChange: {
      action: 'Change detected! Current page is: ',
    },
  }}
  decorators={[
    (Item) => (
      <div
        style={{
          maxWidth: '400px',
        }}
      >
        <Item />
      </div>
    ),
  ]}
/>

# CompactPager

Doesn't exist in Design System yet

**Component status: Experimental**

## Default

<Canvas>
  <Story name="Default" args={{ page: 1, pageSize: 20, collectionSize: 60 }}>
    {(args) => <CompactPager {...args} />}
  </Story>
</Canvas>
