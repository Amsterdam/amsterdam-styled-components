import { Fragment } from 'react'
import { Meta, Story, Canvas } from '@storybook/addon-docs'
import { Hidden } from '@amsterdam/asc-ui'

<Meta title="Headless/Hidden" component={Hidden} />

# Hidden

A (headless) component that you can wrap around other components to decide if they should render according to a specific mediaQuery. This component, as it is headless, doesn't render an html element, only the children or nothing.
This component uses the [useMatchMedia](https://github.com/Amsterdam/amsterdam-styled-components/tree/main/packages/asc-ui/src/utils/hooks/useMatchMedia.ts).

## With min breakpoint

<Canvas>
  <Story name="With min breakpoint">
    <Fragment>
      <Hidden minBreakpoint="tabletM">I am hidden from tabletM</Hidden>
    </Fragment>
  </Story>
</Canvas>

## With max breakpoint

<Canvas>
  <Story name="With max breakpoint">
    <Fragment>
      <Hidden maxBreakpoint="tabletM">I am hidden until tabletM</Hidden>
    </Fragment>
  </Story>
</Canvas>

## With min and max breakpoint

<Canvas>
  <Story name="With min and max breakpoint">
    <Fragment>
      <Hidden minBreakpoint="mobileS" maxBreakpoint="laptop">
        I am hidden only between mobileS and laptop
      </Hidden>
    </Fragment>
  </Story>
</Canvas>

## With a custom query

<Canvas>
  <Story name="With a custom query">
    <Fragment>
      <Hidden query="(min-width: 300px) and (max-width: 600px)">
        I am hidden only between 300px and 600px
      </Hidden>
    </Fragment>
  </Story>
</Canvas>
