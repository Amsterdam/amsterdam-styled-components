import { Meta, Story, Canvas } from '@storybook/addon-docs'
import { Button } from '@amsterdam/asc-ui'
import styled from 'styled-components'
import { Download, Close } from '@amsterdam/asc-assets'

<Meta
  title="UI/Form/Button"
  component={Button}
  argTypes={{
    small: {
      control: { type: 'boolean' },
    },
  }}
/>

# Button

[Design System](https://amsterdam.nl/designsystem/design-bibliotheek/atomen-moleculen/button/)

**Component status: experimental**

## Default / Primary variant

export const ButtonBar = styled.div`
  display: flex;
  flex-wrap: wrap;
  & > * {
    margin-right: 5px;
    margin-bottom: 5px;
  }
`

<Canvas>
  <Story name="Default / Primary variant" args={{ small: false }}>
    {({ small }) => (
      <ButtonBar>
        <Button variant="primary" small={small}>
          Normal
        </Button>
        <Button variant="primary" small={small} disabled>
          Disabled
        </Button>
        <Button
          variant="primary"
          small={small}
          iconSize={small ? 24 : 32}
          iconLeft={<Download />}
        >
          With icon
        </Button>
      </ButtonBar>
    )}
  </Story>
</Canvas>

## Secondary variant

<Canvas>
  <Story name="Secondary variant" args={{ small: false }}>
    {({ small }) => (
      <ButtonBar>
        <Button variant="secondary" small={small}>
          Normal
        </Button>
        <Button variant="secondary" small={small} disabled>
          Disabled
        </Button>
        <Button
          variant="secondary"
          small={small}
          iconSize={small ? 24 : 32}
          iconLeft={<Download />}
        >
          With icon
        </Button>
      </ButtonBar>
    )}
  </Story>
</Canvas>

## Tertiary variant

<Canvas>
  <Story name="Tertiary variant" args={{ small: false }}>
    {({ small }) => (
      <ButtonBar>
        <Button variant="tertiary" small={small}>
          Normal
        </Button>
        <Button variant="tertiary" small={small} disabled>
          Disabled
        </Button>
        <Button
          variant="tertiary"
          small={small}
          iconSize={small ? 24 : 32}
          iconLeft={<Download />}
        >
          With icon
        </Button>
      </ButtonBar>
    )}
  </Story>
</Canvas>

## With only an icon

<Canvas>
  <Story name="With only an icon">
    <Button variant="blank" iconSize={32} icon={<Close />} />
  </Story>
</Canvas>
