import { Meta, Story, Canvas } from '@storybook/addon-docs'
import { Button, ShareButton } from '@amsterdam/asc-ui'
import styled from 'styled-components'
import {
  Download,
  Close,
  Facebook,
  Twitter,
  Linkedin,
  Email,
  Print,
} from '@amsterdam/asc-assets'

<Meta title="UI/Form/Button" component={Button} />

# Button

[Design System](https://designsystem.amsterdam.nl/7awj1hc9f/p/25ff19-buttons)

**Component status: experimental**

## Default / Primary variant

export const ButtonBar = styled.div`
  display: flex;
  flex-wrap: wrap;
  & > * {
    margin-right: 5px;
    margin-bottom: 5px;
  }
`

<Canvas>
  <Story name="Default / Primary variant">
    <ButtonBar>
      <Button variant="primary">Normal</Button>
      <Button variant="primary" disabled>
        Disabled
      </Button>
      <Button variant="primary" iconSize={14} iconLeft={<Download />}>
        With icon
      </Button>
    </ButtonBar>
  </Story>
</Canvas>

## Secondary variant

<Canvas>
  <Story name="Secondary variant">
    <ButtonBar>
      <Button variant="secondary">Normal</Button>
      <Button variant="secondary" disabled>
        Disabled
      </Button>
      <Button variant="secondary" iconSize={14} iconLeft={<Download />}>
        With icon
      </Button>
      <Button variant="secondary" taskflow>
        Taskflow state
      </Button>
    </ButtonBar>
  </Story>
</Canvas>

## Tertiary variant

<Canvas>
  <Story name="Tertiary variant">
    <ButtonBar>
      <Button variant="tertiary">Normal</Button>
      <Button variant="tertiary" disabled>
        Disabled
      </Button>
      <Button variant="tertiary" iconSize={14} iconLeft={<Download />}>
        With icon
      </Button>
    </ButtonBar>
  </Story>
</Canvas>

## Primary Inverted variant

<Canvas>
  <Story name="Primary Inverted variant">
    <ButtonBar>
      <Button variant="primaryInverted">Normal</Button>
      <Button variant="primaryInverted" disabled>
        Disabled
      </Button>
      <Button variant="primaryInverted" iconSize={14} iconLeft={<Download />}>
        With icon
      </Button>
    </ButtonBar>
  </Story>
</Canvas>

## Text Button variant

<Canvas>
  <Story name="Text Button variant">
    <ButtonBar>
      <Button variant="textButton">Normal</Button>
      <Button variant="textButton" disabled>
        Disabled
      </Button>
      <Button variant="textButton" iconSize={14} iconLeft={<Download />}>
        With icon
      </Button>
    </ButtonBar>
  </Story>
</Canvas>

## With only an icon

<Canvas>
  <Story name="With only an icon">
    <Button size={60} variant="blank" iconSize={40} icon={<Close />} />
  </Story>
</Canvas>

## Social buttons

<Canvas>
  <Story name="Social buttons">
    <ButtonBar>
      <ShareButton hoverColor="#3b5999" iconSize={30}>
        <Facebook />
      </ShareButton>
      <ShareButton hoverColor="#55acee">
        <Twitter />
      </ShareButton>
      <ShareButton hoverColor="#0077B5">
        <Linkedin />
      </ShareButton>
      <ShareButton>
        <Email />
      </ShareButton>
      <ShareButton>
        <Print />
      </ShareButton>
    </ButtonBar>
  </Story>
</Canvas>
